# Liquibase 기본 설정
# 환경별 설정은 liquibase-{env}.properties 파일 사용

# Changelog 설정
changeLogFile=changelog/changelog-master.xml
liquibase.hub.mode=off

# 로깅
logLevel=INFO

# 기본값 (환경변수로 오버라이드 가능)
# driver, url, username, password는 환경별 설정 파일에서 정의
